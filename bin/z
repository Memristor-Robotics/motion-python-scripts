#!/usr/bin/python
import sys
sys.path.append('/home/pi/python')
from send import *
send('i')

if len(sys.argv) == 2:
    num = -int(sys.argv[1])
    send("D" + pack(num) + chr(0))
else:
    send("D" + ''.join(chr(x) for x in [0xfe, 0, 0]))
