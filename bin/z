#!/usr/bin/python3
import os,sys
sys.path.append(os.path.join(os.path.dirname(os.path.realpath(__file__)), os.pardir, 'python'))
from Engine import *
e=Engine()
e.send(b'i')

if len(sys.argv) == 2:
	num = -int(sys.argv[1])
	e.send(b"D" + pack(num) + uchr(0))
else:
	e.send(b"D" + b'\xfe\x00\x00')
