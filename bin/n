#!/usr/bin/python
import sys
sys.path.append('/home/pi/python')
from send import *
send('i')
if len(sys.argv) == 2:
    send("D" + pack(int(sys.argv[1])) + chr(0))
else:
    send("D" + pack(512) + chr(0))
