#!/usr/bin/python3
import os,sys
sys.path.append(os.path.join(os.path.dirname(os.path.realpath(__file__)), os.pardir, 'python'))
from Engine import *
e=Engine()

e.send(b'i')
if len(sys.argv) == 2:
	e.send(b'D' + pack(int(sys.argv[1])) + b'\x00')
else:
	e.send(b'D' + pack(512) + b'\x00')
