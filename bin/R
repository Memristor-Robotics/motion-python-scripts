#!/usr/bin/python3

import os,sys
sys.path.append(os.path.join(os.path.dirname(os.path.realpath(__file__)), os.pardir, 'python'))
from Engine import *

e = Engine()
angle = int(sys.argv[1])
e.send(b'i')
e.send(b'A' + struct.pack('>h', angle))
