#!/usr/bin/python

import io
import sys
import struct
sys.path.append("/home/pi/python")
from send import *
#f=io.open("/dev/serial0", "wb")

angle = int(sys.argv[1])

#f.write(b'i')

#f.write(b'A' + str(struct.pack('>h', angle)))

send('i')
send('A' + str(struct.pack('>h', angle)))
